/**
 * Engine-package constants.
 *
 * Cross-workspace note: `src/constants.ts` in the CLI workspace mirrors
 * the daemon-port value. The two stay in sync manually because CLI and
 * engine are independent npm workspaces with separate module systems.
 */

export const DEFAULT_DAEMON_PORT = 7801;
